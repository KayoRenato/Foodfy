{% extends "layout.njk" %}

{% import "parts/header.njk" as header %} 
{% import "parts/card.njk" as card %} 

  {% block head %}
    <title>FoodFy - {{item.title}} </title>
  {% endblock %}

  {% block header %}
    {% call header.render(register) %}
      <a href="/admin/recipes">RECEITAS</a>
      <a href="/admin/chefs">CHEFS</a>  
    {% endcall %}
  {% endblock %}

  {% block main %}

    {% call card.render( item.title, 'recipe-show', id) %}
      
      <div class="recipe">
        <div class="head">
         <div 
            class="image"
            style="background-image: url({{item.image}})"
          ></div>
          <h2>{{item.title}}</h2>
          <p class='author'>por {{item.chef_name}}</p>
        </div>

        <div class="content">
          <div class="ingre">
            <h3>Ingredientes</h3>
              <ul>
                {% for ing in item.ingredients %}
                <li>{{ing}}</li>
                {% endfor %}
              </ul>
          </div>

          <div class="prep">
            <h3>Preparo</h3>
            <ol>
            {% for prep in item.preparation %}
              <li>{{prep}}</li>
              {% endfor %}
            </ol>
          </div>

          {% if item.information %}
            <div class="info">
              <h3>Informações Adicionais</h3>
              <p>{{item.information}}</p>
            </div>
          {% endif %}
              
        </div>  

      </div>  
      
    {% endcall %}

  {% endblock %}